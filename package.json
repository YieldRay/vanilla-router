{
    "name": "vanilla-router",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^5.0.4"
    },
    "dependencies": {
        "urlpattern-polyfill": "^9.0.0"
    }
}
